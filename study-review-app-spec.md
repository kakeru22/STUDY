# 一問一答復習アプリ 仕様書

## 1. 目的

個人利用を前提に、勉強した問題を `PC` と `スマホ` の両方で登録・復習できるアプリを作る。

要件:

- Google Drive を使って同期できる
- 個人用なので、複雑な権限管理は不要
- 問題形式は主に `〇×` と `N択`
- 問題登録をできるだけ手軽にしたい
- CSV 依存は避けたい

結論として、以下の形で進める。

- アプリ形態: `1つのPWA`
- 画面構成: `問題管理` と `復習` を中心にした複数画面
- 同期先: `Google Driveの見える専用フォルダ`
- ローカル保存: `IndexedDB`
- データ形式: `JSON`

## 2. アプリ全体像

### 2.1 基本構成

1つのPWAの中に以下の画面を持たせる。

- `ホーム`
- `問題追加`
- `問題一覧 / 編集`
- `復習`
- `同期 / 設定`

この構成にする理由:

- 2アプリ構成より管理が楽
- PCでもスマホでも同じURLで使える
- 同期処理を1か所に集約できる

### 2.2 想定利用フロー

1. Googleでログイン
2. 初回起動時に Drive 上へ `StudyReviewApp` フォルダを作成
3. 問題をフォームで登録
4. ローカルに即保存
5. 一定タイミングまたは手動で Drive に同期
6. 復習画面で出題
7. 正誤に応じて進捗更新
8. 更新内容を Drive に同期

## 3. 問題形式の設計

### 3.1 結論

`〇×` も `N択` も、内部的にはすべて `択形式` として扱う。

つまり:

- `〇×問題` = `2択問題`
- `N択問題` = `2択以上の通常選択問題`

この設計を推奨する理由:

- 出題UIを1種類に統一できる
- 正解判定ロジックを共通化できる
- データ構造が単純になる
- 今後 `3択`, `4択`, `5択` が混ざっても拡張しやすい

### 3.2 〇×問題の扱い

`〇×` のときは、入力画面で `〇` と `×` を自動入力する。

実装方針:

- 問題タイプで `〇×` を選ぶ
- 選択肢欄は固定で `〇` と `×`
- ユーザーは `どちらが正解か` だけ選ぶ
- 通常は選択肢文言を編集不可にする

これがよい理由:

- 入力が速い
- `○/〇` や `×/✕` の表記ゆれを防げる
- 復習時の表示が揃う

### 3.3 N択問題の扱い

N択問題では、選択肢を `2〜6件` まで可変にする。

初期値:

- デフォルトは `4択`
- 必要なら `選択肢を追加`
- 不要なら `選択肢を削除`

MVPでは `単一正解` のみ対応とする。

将来拡張:

- 複数選択問題
- 記述問題
- 穴埋め問題

## 4. データモデル

### 4.1 問題データ

```json
{
  "id": "q_20260723_0001",
  "type": "binary",
  "category": "英単語",
  "questionText": "apple は『りんご』である。",
  "choices": [
    { "id": "a", "label": "〇" },
    { "id": "b", "label": "×" }
  ],
  "correctChoiceId": "a",
  "explanation": "apple = りんご",
  "tags": ["英語", "単語", "基礎"],
  "difficulty": 1,
  "source": "英単語帳A",
  "createdAt": "2026-07-23T10:00:00+09:00",
  "updatedAt": "2026-07-23T10:00:00+09:00",
  "archived": false
}
```

N択問題の例:

```json
{
  "id": "q_20260723_0002",
  "type": "multiple",
  "category": "歴史",
  "questionText": "鎌倉幕府の成立年として最も一般的に扱われるものを選べ。",
  "choices": [
    { "id": "a", "label": "1180年" },
    { "id": "b", "label": "1185年" },
    { "id": "c", "label": "1192年" },
    { "id": "d", "label": "1200年" }
  ],
  "correctChoiceId": "c",
  "explanation": "学習方針に応じて1185年説もあるため、補足表示できるようにする。",
  "tags": ["歴史", "鎌倉"],
  "difficulty": 2,
  "source": "日本史ノート",
  "createdAt": "2026-07-23T10:10:00+09:00",
  "updatedAt": "2026-07-23T10:10:00+09:00",
  "archived": false
}
```

必須項目:

- `id`
- `type`
- `questionText`
- `choices`
- `correctChoiceId`

推奨項目:

- `category`
- `explanation`
- `tags`
- `difficulty`
- `source`

### 4.2 学習進捗データ

問題本体とは分けて管理する。

```json
{
  "questionId": "q_20260723_0002",
  "attempts": 8,
  "correctCount": 5,
  "wrongCount": 3,
  "correctStreak": 2,
  "lastAnsweredAt": "2026-07-23T21:15:00+09:00",
  "lastResult": "correct",
  "nextReviewAt": "2026-07-24T21:15:00+09:00",
  "isStarred": true
}
```

### 4.3 設定データ

```json
{
  "userDisplayName": "User",
  "driveFolderId": "xxxxxxxx",
  "theme": "system",
  "defaultReviewMode": "today",
  "defaultChoiceCount": 4,
  "autoSync": true,
  "lastSyncedAt": "2026-07-23T21:20:00+09:00"
}
```

### 4.4 ローカル同期メタデータ

Drive同期そのものとは別に、端末内で同期状態を持つ。

```json
{
  "lastLocalChangeAt": "2026-07-23T21:18:00+09:00",
  "lastSuccessfulSyncAt": "2026-07-23T21:20:00+09:00",
  "questionsRemoteRevision": "rev_questions_001",
  "progressRemoteRevision": "rev_progress_001",
  "hasUnsyncedChanges": true,
  "syncMode": "offline"
}
```

用途:

- 未同期変更があるか判定する
- Drive側ファイルが前回同期時から変わったか判定する
- オフライン中かオンライン中かをUIに表示する

## 5. Google Drive保存構成

Drive上の `StudyReviewApp` フォルダ内に以下を保存する。

- `questions.json`
- `progress.json`
- `settings.json`
- `backups/`

### 5.1 ファイルの役割

- `questions.json`
  問題マスタ
- `progress.json`
  正誤履歴と復習予定
- `settings.json`
  ユーザー設定と同期メタ情報
- `backups/`
  上書き前の自動バックアップ

### 5.2 保存の考え方

セッション中の主データは `IndexedDB` に置く。

同期時に:

- ローカル -> Drive へ保存
- 起動時または手動で Drive -> ローカルへ読込

この構成にする理由:

- スマホでも体感が軽い
- オフラインでも入力できる
- Drive障害時も一時利用できる

### 5.3 オフライン起動

アプリは `オフライン優先` で起動する。

起動時の流れ:

1. まず `IndexedDB` から問題・進捗・設定を読み込む
2. 画面をすぐ表示する
3. 裏でネットワーク状態とGoogle認証状態を確認する
4. Driveへ接続できれば同期候補を判定する
5. 接続できなければ `オフラインモード` のまま使える

この設計により、通信環境が悪くても既存キャッシュで起動できる。

### 5.4 オフライン中の保存

オフライン中に行った操作は、すべて `IndexedDB` に即保存する。

対象:

- 新しい問題の追加
- 問題の編集
- 問題の削除
- 解答結果の記録
- 設定変更

ユーザーには `未同期` と表示し、オンライン復帰後に同期できる状態にする。

## 6. 同期ルール

### 6.1 基本方針

個人用のため、同期ルールは単純化する。

- 同時編集は基本想定しない
- ただし別端末で更新した場合の競合検知は行う
- 競合時はユーザーに選ばせる
- 起動・操作の基準データは常に `IndexedDB`

### 6.1.1 同期の考え方

ユーザー視点では、以下が同期される見え方になる。

- オフライン中に正解 / 不正解を付けた履歴
- オフライン中に追加した新しい問題
- オフライン中に編集した問題

ただし内部実装としては、Driveへ `個別イベント` を1件ずつ送る必要はない。

MVPでは以下の方式にする。

- 端末上では変更をすべて `IndexedDB` に反映
- 同期時に `questions.json` と `progress.json` の最新内容をまとめてDriveへ保存

つまり `ユーザー体験は差分同期` に見せつつ、`実装はスナップショット同期` とする。

この方式を選ぶ理由:

- DriveはDBではなくファイル保存なので単純実装の方が壊れにくい
- 個人用ではファイル全体再保存でも現実的
- 競合判定が分かりやすい

### 6.2 同期タイミング

- アプリ起動時に Drive の更新有無を確認
- 変更後にローカル保存
- `同期` ボタンで Drive に即反映
- 設定で `自動同期ON` の場合は数秒遅延後に同期

### 6.2.1 オフライン時の動作

オフライン時:

- 起動は `IndexedDB` から行う
- 問題追加や復習は通常通り使える
- 変更はローカル保存のみ行う
- ヘッダーや設定画面に `オフライン中 / 未同期あり` を表示する

### 6.2.2 オンライン復帰時の動作

オンライン復帰時:

1. `オンラインに戻る` または `今すぐ同期` を押す
2. Google認証が有効か確認
3. Drive上のファイル更新時刻とリビジョンを確認
4. 競合がなければローカルの最新内容をDriveへ保存
5. 同期成功後に `未同期` 状態を解除

つまり、ユーザーのイメージどおり `正解したかどうか` と `新しい問題` はこの時点で反映される。

### 6.2.3 自動オンライン判定

手動ボタンに加えて、自動判定も行う。

- ブラウザの `online/offline` イベントを監視
- ただし `online` でも実際はDrive接続失敗することがある
- そのため最終的には `Drive APIへ疎通できたか` でオンライン扱いを確定する

### 6.3 競合時の処理

競合条件:

- 最終同期以降にローカルも更新
- かつ Drive 側の更新時刻も変わっている

競合時の選択肢:

- `Driveを優先して読み込む`
- `ローカルで上書きする`
- `バックアップを作ってから上書きする`

MVPでは自動マージはしない。

### 6.4 同期単位

MVPで同期する単位は以下の3ファイルとする。

- `questions.json`
- `progress.json`
- `settings.json`

推奨動作:

- 問題追加・編集があったら `questions.json` を更新
- 正誤や復習予定の変更があったら `progress.json` を更新
- 同期時刻やDriveフォルダ情報が変わったら `settings.json` を更新

### 6.5 UI上の同期状態

常に以下のどれかを分かるようにする。

- `オンライン`
- `オフライン`
- `同期中`
- `未同期あり`
- `競合あり`

表示場所:

- 画面上部のステータスバー
- 設定画面
- ホーム画面

## 7. 画面仕様

## 7.1 ホーム

表示内容:

- 今日の復習件数
- 苦手問題件数
- 最近追加した問題数
- 最終同期時刻

主な操作:

- `今日の復習を開始`
- `問題を追加`
- `問題一覧を見る`
- `同期`

## 7.2 問題追加画面

最重要画面。入力しやすさを最優先する。

入力項目:

- 問題タイプ
- 科目 / カテゴリ
- 問題文
- 選択肢
- 正解
- 解説
- タグ
- 難易度
- 出典

### 7.2.1 入力UI

問題タイプ:

- `〇×`
- `N択`

タイプ別挙動:

- `〇×`
  選択肢は `〇` と `×` を自動表示し、正解だけ選ぶ
- `N択`
  2〜6件の選択肢入力欄を表示

### 7.2.2 入力しやすくする工夫

- `連続入力モード`
  保存後も画面遷移せず次の問題を続けて入力
- `前回のカテゴリ/タグを引き継ぐ`
  同じ分野の連続登録が速い
- `4択テンプレート`
  最初から4件分の欄を表示
- `〇×テンプレート`
  選択肢を自動入力
- `タグ候補サジェスト`
  既存タグを候補表示
- `スマホ向け大きめボタン`
  正解選択や保存を押しやすくする
- `必須項目だけ先に表示`
  詳細項目は折りたたみ

### 7.2.3 フォームの推奨レイアウト

上から順に:

1. 問題タイプ切替
2. カテゴリ
3. 問題文
4. 選択肢入力
5. 正解選択
6. 解説
7. タグ
8. 保存ボタン

PCでは2カラムでもよいが、スマホは1カラム固定にする。

### 7.2.4 保存後の挙動

保存後の選択肢:

- `保存して一覧へ`
- `保存して続けて入力`
- `下書き保存`

MVPでは `保存して続けて入力` を強く推奨。

## 7.3 問題一覧 / 編集画面

機能:

- 全問題一覧
- キーワード検索
- タグ絞り込み
- カテゴリ絞り込み
- `〇×` / `N択` 絞り込み
- 編集
- 論理削除
- お気に入り表示

一覧表示項目:

- 問題文の先頭
- カテゴリ
- タグ
- 問題タイプ
- 正答率
- 最終学習日

## 7.4 復習画面

出題方法:

- `今日の復習`
- `ランダム`
- `苦手のみ`
- `タグ指定`
- `カテゴリ指定`

表示順:

1. 問題文
2. 選択肢ボタン
3. 解答結果
4. 解説
5. 次へ

回答後の処理:

- 正誤記録
- 連続正解数更新
- 次回復習日計算

### 7.4.1 復習間隔

MVPの簡易ルール:

- 初回正解: `1日後`
- 2回連続正解: `3日後`
- 3回連続正解: `7日後`
- 4回連続正解: `14日後`
- 5回以上連続正解: `30日後`
- 不正解: `翌日`

## 7.5 同期 / 設定画面

表示内容:

- ログイン中アカウント
- 接続中Driveフォルダ名
- 最終同期日時
- ローカル保存件数
- Drive保存件数

操作:

- `今すぐ同期`
- `Driveから再読込`
- `バックアップ作成`
- `自動同期 ON/OFF`
- `オンラインに戻る`

### 7.5.1 オフラインモードのUI

オフライン中は明確に状態を表示する。

表示例:

- `オフラインモードで起動中`
- `未同期の変更が 12 件あります`
- `最後に同期した日時: 2026-07-23 21:20`

ボタン:

- `オンラインに戻る`
- `今すぐ同期`
- `ローカルデータだけで続ける`

## 8. 入力フォームの最終方針

ユーザーの懸念どおり、CSVは初期MVPでは主手段にしない。

理由:

- 文字コード問題が起きやすい
- スマホで扱いにくい
- 修正が面倒
- 個人用では手入力フォームの方が運用しやすい

そのため入力は以下を主軸にする。

### 8.1 主入力方式

- `アプリ内フォーム`

### 8.2 補助入力方式

- `JSONバックアップからの復元`
- 将来対応として `Googleスプレッドシート取込`

スプレッドシート取込はCSVより現実的で、将来拡張として相性がよい。

## 9. MVP機能一覧

最初のリリースに入れるもの:

- Googleログイン
- Drive専用フォルダ作成
- 問題追加フォーム
- `〇×` と `N択` の保存
- 問題一覧
- 問題編集
- 復習画面
- 正誤記録
- 簡易復習間隔
- IndexedDB保存
- オフライン起動
- オフライン中の問題追加 / 復習
- オンライン復帰時の再同期
- Drive同期
- 手動バックアップ

最初は入れないもの:

- 複数正解問題
- 画像付き問題
- 音声
- 他ユーザー共有
- 自動マージ
- CSV取込

## 10. 技術方針

実装候補:

- フロントエンド: `React + TypeScript`
- UI: `PWA対応Webアプリ`
- ローカルDB: `IndexedDB`
- 認証: `Google Identity Services`
- Drive連携: `Google Drive API`

理由:

- PC/スマホ両対応がしやすい
- 保守しやすい
- Drive連携の実装資産が多い

## 11. 最終結論

今回の仕様としては以下で確定するのがよい。

- アプリは `1つのPWA`
- 問題形式は `すべて択形式` に統一
- `〇×` は `〇/× が自動入力された2択問題` として扱う
- 入力の主軸は `フォーム`
- 同期は `Google Driveの見えるフォルダ`
- ローカルには `IndexedDB`
- 個人用前提で競合処理は簡易にする

## 12. 次に作るもの

実装前に必要な具体化項目:

1. 画面ワイヤーフレーム
2. JSONスキーマ
3. IndexedDBテーブル設計
4. Drive同期フロー
5. Reactコンポーネント構成

この仕様なら、そのまま実装に入れる。
